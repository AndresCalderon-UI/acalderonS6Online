<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="acalderonS6Online.Views.vEstudiante"
             Title="vEstudiante">
    <VerticalStackLayout
        Padding="20"
        Spacing="20">
        <Label 
            Text="Semana 6: Servicios Web"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

        <StackLayout>

            <Grid Padding="10" ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Label Text="CODIGO" Grid.Column="0" FontAttributes="Bold" />
                <Label Text="NOMBRE" Grid.Column="1" FontAttributes="Bold" />
                <Label Text="APELLIDO" Grid.Column="2" FontAttributes="Bold" />
                <Label Text="EDAD" Grid.Column="3" FontAttributes="Bold" />
            </Grid>

            <ListView x:Name="listaEstudiantes">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid Padding="10" ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <Label Text="{Binding codigo}" Grid.Column="0" FontSize="Small" />
                                <Label Text="{Binding nombre}" Grid.Column="1" FontSize="Small" />
                                <Label Text="{Binding apellido}" Grid.Column="2" FontSize="Small" />
                                <Label Text="{Binding edad}" Grid.Column="3" FontSize="Small" />
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackLayout>
    </VerticalStackLayout>
</ContentPage>